# 缺口盤點

盤點日期：2026-09-17（Asia/Taipei）。盤點依據：`feature/init-project` 於 `8dd81a0` 的實際程式碼、`docs/ARCHITECTURE.md`、`docs/API.md`、`docs/ECPAY.md`、`docs/VALIDATION.md`。

本檔由 PM / SA（Claude）維護。**優先順序是建議，最終由 PO（HSIN）決定。** 規格書寫在 `docs/specs/`，檔名對應缺口編號。

## 現況摘要

初版是一套可運作的模組化單體：登入 / 角色 / 權限、分店、菜單、點餐、現金收款、綠界線上付款、銷售報表都已實作並有測試覆蓋（`CoffeeIntegrationTest`、`HttpWorkflowTest`、`ModuleBoundariesTest`、`CheckMacTest`）。授權、金額後端重算、冪等下單、行鎖、綠界簽章驗證這些硬規則都有落實。

缺口集中在**營運面**，不在功能面：錢收了之後的異常處理、法規要求、以及規模化所需的基礎設施。

## 優先順序

### P0 — 阻擋正式上線（收了錢卻無法處理的情況）

| 編號 | 缺口 | 狀態 | 規格書 |
| --- | --- | --- | --- |
| G01 | 金流對帳與付款狀態修復 | **規格書已完成，待實作** | [`specs/G01-payment-reconciliation.md`](specs/G01-payment-reconciliation.md) |
| G02 | 電子發票 | 未開始 | — |
| G03 | 退款與退單 | 未開始 | — |

**G01 金流對帳與付款狀態修復**

`EcpayService.callback()` 是目前**唯一**把 ECPAY 訂單從 `PENDING_PAYMENT` 推進到 `PAID` 的路徑。後端從未主動連線到綠界（主程式碼裡沒有任何 HTTP client，也沒有 `@Scheduled` / `@EnableScheduling`）。

所以只要那一則回呼沒送達——網路中斷、應用重啟、綠界重送次數用盡——**顧客的卡已經被扣款，訂單卻永遠停在未付款**，而且沒有任何人會知道。`docs/ECPAY.md` 自己也寫了：「對於付款通知遺失須由營運人員依綠界交易紀錄查核；未實作自動對帳前不應把這套初版視為無人值守的正式收款平台。」

這是唯一一個「已經在收真錢、但缺乏補救路徑」的缺口，所以排第一。

**G02 電子發票**

台灣營業人開立統一發票有法規義務。目前完全未實作，也沒有發票號碼、字軌、作廢或折讓的資料模型。正式營業前必須補上，但它高度依賴 PO 選定的加值中心（綠界電子發票 / 其他），技術選型未定前寫規格書意義不大。

**G03 退款與退單**

`ARCHITECTURE.md` 明載「付款後退款與退單不在本版範圍」。訂單狀態機 `PAID → PREPARING → READY → COMPLETED` 是單向的，已付款訂單沒有任何回退路徑；`orders` 表也沒有記錄退款金額的欄位。做錯餐、顧客反悔、店家無法供餐時，現場只能在系統外處理現金，帳就對不起來。

### P1 — 營運必要

| 編號 | 缺口 | 狀態 | 規格書 |
| --- | --- | --- | --- |
| G04 | 線上付款訂單的取消與逾時處理 | 未開始 | — |
| G05 | Session 集中化（水平擴展前提） | 未開始 | — |
| G06 | 商品選項模型與加價 | 未開始 | — |
| G07 | 折扣與促銷 | 未開始 | — |

**G04** —— `OrderService.transition()` 只允許取消 `PENDING_PAYMENT` 且 `paymentMethod='CASH'` 的訂單。ECPAY 訂單一旦建立就無法取消（這是刻意的，避免與異步付款競爭），但也因此沒有出口：顧客放棄付款後訂單永遠掛在未付款清單裡。需要逾時自動作廢的規則，且必須與 G01 的對帳邏輯互斥，不能作廢掉一筆其實已經付款成功的訂單。

**G05** —— 單一實例的 `HttpSession`，應用重啟就全部登出，也無法水平擴展。登入限流同樣是單機記憶體。`ARCHITECTURE.md` 已點名需要 Spring Session / Redis。在單店單機營運下可接受，多店或需要零停機部署時就是硬阻擋。

**G06** —— `order_items.temperature` / `sugar` 是 `VARCHAR(12)` 自由字串，**完全不影響金額**。無法表達「加珍珠 +10」「換燕麥奶 +20」這類實際咖啡廳每天都在賣的加價選項，也無法限制某商品的可選範圍（熱美式不該能選「無糖」以外的糖度）。這是最直接的營收缺口。

**G07** —— 沒有任何折扣機制：無優惠券、無會員價、無買一送一、無員工價。`orders.total` 是純加總。導入時要特別小心，折扣是最容易出現「信任前端傳來金額」漏洞的地方。

### P2 — 規模與體驗

| 編號 | 缺口 | 狀態 |
| --- | --- | --- |
| G08 | 庫存扣減 | 未開始 |
| G09 | 報表效能（月報記憶體彙整） | 未開始 |
| G10 | 訂單清單分頁 | 未開始 |
| G11 | 稽核紀錄的查詢與涵蓋範圍 | 未開始 |
| G12 | 外送、硬體印單、會員自助註冊 | 未開始 |

**G09** —— `ARCHITECTURE.md` 自述月報把整月已付款訂單投影載入記憶體，以 Java 彙整。單店資料量下沒問題，跨店或資料累積後會變成記憶體與延遲風險。

**G10** —— `OrderService` 第 136 行 `order by created_at desc limit 100` 是寫死的。超過 100 筆的歷史訂單在 UI 上完全看不到，也沒有日期篩選或分頁參數。

**G11** —— `audit_log` 表存在，但全專案只有 `IdentityService` 第 221 行一處寫入，且沒有任何查詢端點。等於有稽核資料卻無法稽核。金額相關操作（現金收款、狀態轉換、未來的退款與折扣）都應該進稽核軌跡。

## 下一步建議

1. Codex 依 `specs/G01-payment-reconciliation.md` 實作對帳機制（P0，已可開工）
2. PO 決定 G02 電子發票的加值中心選型，選定後 Claude 產出規格書
3. G03 退款規格書（建議與 G02 一起設計，退款會連動折讓發票）
4. G06 選項模型（P1 中最直接的營收缺口，且不依賴外部服務，可與 P0 並行）

## 修訂紀錄

| 日期 | 變更 |
| --- | --- |
| 2026-09-17 | 建立本檔；完成 G01 規格書 |
